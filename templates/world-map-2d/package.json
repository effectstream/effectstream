{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:evm": "bun run --filter @world-map-2d/contracts-evm build:mod",
    "dev": "NODE_ENV=development bunx orchestrator start",
    "check": "bun run --filter @world-map-2d/node check",
    "test": "bun run packages/tests/run-tests.ts",
    "build:pgtypes": "bun run --filter @world-map-2d/database pgtyped:update"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.3.10",
    "@effectstream/evm-contracts": "0.104.0",
    "@midnightntwrk/wallet-sdk-address-format": "3.1.2",
    "@effectstream/orchestrator": "0.104.0",
    "wait-on": "8.0.3"
  },
  "effectstream": {
    "default": "start.dev.ts"
  }
}
