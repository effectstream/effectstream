{
  "name": "@dice/evm-contracts",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./mod.ts",
    "./build": "./build/mod.ts",
    "./contractAddressesEvmMain": "./build/contractAddressesEvmMain.ts"
  },
  "scripts": {
    "swap:remappings:forge": "bunx @paimaexample/evm-hardhat/remappings-forge --depth 4 --package @paimaexample",
    "swap:remappings:hardhat": "bunx @paimaexample/evm-hardhat/remappings-hardhat",
    "check": "echo 'check'",
    "build:mod": "bun run build && mkdir -p build && echo '\\n// this is a auto-generated file, do not edit it manually\\n\\nimport path from \"node:path\";\\nconst __dirname = import.meta.dirname ?? \"\";\\nexport const contracts = {\\n} as const;' > build/contracts.ts && echo '// this is a auto-generated file, do not edit it manually\\n\\nexport { contracts } from \"./contracts.ts\";' > build/mod.ts && bunx @paimaexample/evm-hardhat/builder",
    "build:clean": "rm -rf build || true",
    "build:forge": "bun run swap:remappings:forge && forge build",
    "build:hardhat": "bun run swap:remappings:hardhat && bunx hardhat compile",
    "build": "bun run build:clean && bun run build:forge && bun run build:hardhat",
    "contract:compile": "bun run build:mod",
    "deploy:clean": "rm -rf ignition/deployments || true",
    "deploy:standalone": "bun run build && (((bun run chain:start) &) && (bun run chain:wait) && (bun run deploy) && bun run chain:stop) || true",
    "deploy": "bun run deploy-simple.ts",
    "chain:start": "bunx hardhat node",
    "chain:stop": "kill -9 $(lsof -ti tcp:8545) || true",
    "chain:wait": "bunx hardhat node wait"
  },
  "dependencies": {
    "@effectstream/evm-hardhat": "0.100.18",
    "@effectstream/log": "0.100.18",
    "@effectstream/evm-contracts": "0.100.18",
    "@openzeppelin/contracts": "5.1.0",
    "@openzeppelin/contracts-upgradeable": "^5.1.0",
    "@wagmi/cli": "2.3.1",
    "hardhat": "3.0.4",
    "@nomicfoundation/hardhat-ignition-viem": "3.0.2",
    "@nomicfoundation/hardhat-ignition": "3.0.2",
    "@nomicfoundation/hardhat-viem": "3.0.0",
    "@nomicfoundation/ignition-core": "3.0.2",
    "ws": "8.18.1",
    "@nomicfoundation/hardhat-errors": "3.0.1",
    "@nomicfoundation/hardhat-utils": "3.0.0",
    "ethers": "6.16.0"
  },
  "devDependencies": {
    "jsonc-parser": "3.3.1",
    "@opentelemetry/sdk-node": "0.56.0",
    "wait-on": "8.0.3"
  }
}
