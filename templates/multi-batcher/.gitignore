# Written by link.sh — carries this machine's absolute MONOREPO_ROOT.
.env

# Runtime artifacts from a local stack.
batcher-data/
midnight-level-db*/
product-a/contract-counter.*.json

# Compact compiler output (regenerate with `bun run compile:contract`).
product-a/contract-counter/src/managed/
