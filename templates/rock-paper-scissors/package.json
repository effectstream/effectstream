{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:evm": "bun run --filter @rock-paper-scissors/contracts-evm build:mod",
    "dev": "NODE_ENV=development bunx orchestrator start",
    "check": "bun run --filter @rock-paper-scissors/node check",
    "test": "bun run packages/tests/run-tests.ts",
    "build:pgtypes": "bun run --filter @rock-paper-scissors/database pgtyped:update"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.3.10",
    "@effectstream/evm-contracts": "0.100.18",
    "@effectstream/orchestrator": "0.100.18",
    "wait-on": "8.0.3"
  },
  "effectstream": {
    "default": "start.dev.ts"
  }
}
